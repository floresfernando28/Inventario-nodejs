// Importamos la conexión ala BD ===>
const conexion = require('../Databases/ConexionDB');

// Controladores donde se haran todas las ejecuciones
exports.Register = (req, res)=>{
   console.log('Desde el controlador Register')
}